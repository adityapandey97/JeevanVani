PORT=5000
NODE_ENV=development
JWT_SECRET=jeevanvani_pmajay_super_secret_jwt_key_2026

# PostgreSQL Configuration (Optional - falls back to SQLite if not reachable)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/jeevanvani
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=postgres
# PGDATABASE=jeevanvani

# AI / LLM Provider Configuration (Optional - uses built-in NLP engine if empty)
# GEMINI_API_KEY=
# OPENAI_API_KEY=
